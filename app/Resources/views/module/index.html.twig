

        <thead>
            <tr>

                <th>Nom</th>

                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for module in modules %}
            <tr>
                <td>{{ module.nom }}</td>
                <td>
                    <a href="{{ path('module_edit', { 'id': module.id }) }}" class="btn btn-default" ><i class="fa fa-pencil"></i></a>
                    <a href="{{ path('module_edit', { 'id': module.id }) }}" class="btn btn-default" ><i class="fa fa-trash"></i></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>


