{% extends 'base.html.twig' %}
{% block body %}
    <div class="content-wrapper">
        <div class="container">
            <section class="content-header"><h1>Sélection du module</h1></section>
            <section class="content">
                <div class="row">
                    {% for module in modules %}

                            <div class="col-lg-3 col-xs-6">
                                <!-- small box -->
                                <div class="small-box bg-green">
                                    <div class="inner">
                                        <h3>{{ module.nom }}</h3>


                                    </div>
                                    <div class="icon">
                                        <i class="ion ion-stats-bars"></i>
                                    </div>
                                    <a href="{{ path('goout', {'equipe': equipe.id, 'module': module.id}) }}" class="small-box-footer">
                                        Choisir <i class="fa fa-arrow-circle-right"></i>
                                    </a>
                                </div>
                            </div>

                        {%  endfor %}
                </div>
            </section>
        </div>
    </div>
{% endblock %}